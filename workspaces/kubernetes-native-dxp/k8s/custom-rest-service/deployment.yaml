apiVersion: apps/v1
kind: Deployment
metadata:
  name: custom-rest-service
  labels:
    app: custom-rest-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: custom-rest-service
  template:
    metadata:
      labels:
        app: custom-rest-service
    spec:
      containers:
      - name: custom-rest-service
        image: custom-rest-service
        imagePullPolicy: Never
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 8081
        - containerPort: 8000
        env:
          - name: DXP_SERVICE_URI
            valueFrom:
              configMapKeyRef:
                key: service_uri
                name: custom-rest-service.user.agent.oauth2.liferay.com
          - name: OAUTH2_INTROSPECTION_URI
            valueFrom:
              configMapKeyRef:
                key: introspection_uri
                name: custom-rest-service.user.agent.oauth2.liferay.com
          - name: OAUTH2_USERAGENTAPP_CLIENT_ID
            valueFrom:
              configMapKeyRef:
                key: client_id
                name: custom-rest-service.user.agent.oauth2.liferay.com
